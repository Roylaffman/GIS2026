# Secrets — never commit API keys
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Node / npm
node_modules/
.npm-cache/
frontend/dist/

# GIS / raster intermediates (re-generable)
*.aux.xml
cache/
temp/

# Data artifacts that are regenerated by scripts (keep sources small)
data/gis.duckdb
data/gis.duckdb.wal
data/duckdb_ext/
data/smoke_dem.tif
data/pois_raw.json

# Debug / scratch
data/chrome_profile/
*.log
